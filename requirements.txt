# Podcast Creator Agent - Dependencies

# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0

# Web scraping and content fetching
requests>=2.31.0
beautifulsoup4>=4.12.0

# RSS feed parsing
feedparser>=6.0.10

# PDF ingestion (Step 21 - Phase 5)
# pypdf>=5.9.0 is not available on PyPI; pin to latest available
pypdf>=5.9.0

# YouTube transcript ingestion (Step 22 - Phase 5)
youtube-transcript-api>=0.6.0

# Multi-provider support (Step 21 - Phase 5A)
# google-genai>=0.1.0  # optional provider (install manually if needed)

# Voice cloning (Step - ElevenLabs)
elevenlabs>=1.0.0

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0


